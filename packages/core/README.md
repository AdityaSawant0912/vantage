# usevantage

Core of Vantage — a self-hosted web analytics library. Zero infra
dependencies: `Handler`, `processEvent`, and the `QueueAdapter`/
`StoreAdapter` interfaces that real adapter packages (`adapter-memory`,
`adapter-redis`, `adapter-postgres`) implement.

See the [Vantage docs](../../apps/docs) for the full API reference and
adapter contract guide.
