{
  "name": "@usevantage/adapter-memory",
  "version": "0.0.1",
  "description": "In-process QueueAdapter + StoreAdapter — the zero-infra default for @usevantage/core.",
  "type": "module",
  "sideEffects": false,
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@usevantage/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@usevantage/config": "workspace:*",
    "@usevantage/core": "workspace:*",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
