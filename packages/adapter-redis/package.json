{
  "name": "@vantage/adapter-redis",
  "version": "0.0.1",
  "description": "Redis-backed QueueAdapter for usevantage — pulled in only at scale.",
  "type": "module",
  "sideEffects": false,
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ioredis": "^5.4.1"
  },
  "peerDependencies": {
    "usevantage": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vantage/config": "workspace:*",
    "usevantage": "workspace:*",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
